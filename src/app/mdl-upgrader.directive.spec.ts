import { MdlUpgraderDirective } from './mdl-upgrader.directive';

describe('MdlUpgraderDirective', () => {
  it('should create an instance', () => {
    const directive = new MdlUpgraderDirective();
    expect(directive).toBeTruthy();
  });
});
